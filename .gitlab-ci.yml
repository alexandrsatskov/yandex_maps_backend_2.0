image: snakepacker/python:all

test:
  services:
    - docker:dind
  variables:
    CI_MAPS_PG_URL: postgresql+asyncpg://user:hackme@localhost/maps
  script:
    - make devenv
    - make postgres
    - make pytest
